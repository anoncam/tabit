version: '3.8'

services:
  ytdlp-service:
    build: .
    ports:
      - "8080:8080"
    environment:
      - API_KEY=${API_KEY:-dev-secret-key}
    volumes:
      - ./app.py:/app/app.py  # For development hot-reload
    restart: unless-stopped
